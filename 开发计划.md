# 智能 CRM 系统后端开发落地计划 (轻量单机版)

作为项目架构师，针对**单机部署、无 Redis 依赖、采用 Sa-Token 鉴权**的敏捷开发场景，我们的设计核心是：**压榨本地内存（如 Sa-Token 的本地缓存组件）的高效性，利用 Spring 内部事件机制替代分布式消息，确保整体架构极简、好维护。**

---

## 一、 核心技术栈选型

针对单机轻量级架构，技术栈全面向“开箱即用、低运维成本”靠拢：

* **核心框架**：Spring Boot 3.x + JDK 17 +vue3 +ElementPlus
* **安全与鉴权**：**Sa-Token（最新轻量版）**
    * *选型理由*：不依赖 Redis 时，Sa-Token 默认使用本地内存存储 Token 映射，其提供的 `StpUtil` 权限拦截和路由拦截器非常轻量，完美契合单机应用。
* **ORM 框架**：MyBatis-Plus
    * *选型理由*：利用其强大的拦截器机制（`DataPermissionInterceptor`）来实现 PRD 中要求的数据可见范围隔离。
* **数据库**：MySQL 8.0（所有关联均采用**逻辑外键**，利用物理索引 `KEY` 优化查询）。
* **本地缓存**：Sa-Token 默认内存存储（或按需引入 Caffeine），用于存放临时登录态及权限列表。
* **定时任务**：Spring 自带的 **`@Scheduled`**（单机部署无需分布式任务调度引擎，自带定时器即可满足凌晨的数据处理）。

---

## 二、 开发计划与业务落地拆解

根据“**核心且简单优先 > 核心且难优先 > 边缘且简单 > 边缘且难**”的架构原则，开发周期划分为四个阶段（已剔除纯常规 CRUD 逻辑）。

### 阶段一：基石与骨架构建（最高优先级）
> **策略**：虽然 Sa-Token 接入很简单，但**动态数据隔离**是 CRM 的灵魂，必须在第一行具体业务代码编写前在底层立住，否则后期重构成本极大。

1. **Sa-Token 基础权限拦截（简单 - 核心）**
   * **业务实现**：配置 Sa-Token 全局过滤器/拦截器，拦截未登录请求。
   * **逻辑**：实现 `StpInterface` 接口。当用户登录成功后，Sa-Token 会自动调用该接口。从数据库（`sys_user_role` & `sys_role_menu`）中捞出当前用户的角色和权限标识（如 `crm:customer:delete`）并缓存在本地内存中，供 `@SaCheckPermission` 注解进行鉴权。

2. **MyBatis-Plus 动态数据权限拦截（困难 - 核心）**
   * **业务实现**：编写自定义的 `DataPermissionHandler`。
   * **逻辑**：通过 Sa-Token 的 `StpUtil.getLoginId()` 获取当前登录销售的 ID。解析其角色的 `data_scope`。
     * 若为“仅本人（5）”，动态在客户、商机等 SQL 末尾强行拼接 `WHERE owner_user_id = 个人ID`。
     * 若为“本部门（3）”，则拼接 `WHERE owner_user_id IN (本部门用户ID列表)`。

---

### 阶段二：客户资产流转跟进（高优先级）
> **策略**：打通核心主干业务流，让销售人员能够录入、跟进和转化客户。

1. **线索一键转客户（简单 - 核心）**
   * **业务实现**：使用 Spring 的声明式事务（`@Transactional`）。
   * **逻辑**：接收线索 ID，校验其状态是否为未转化。从 `crm_lead` 复制数据，同步向 `crm_customer` 和 `crm_contact` 表双写数据。成功后将原线索状态更新为 `3-已转客户`。
   * *单机优势*：单机本地事务，无需考虑分布式事务，`@Transactional` 即可保证 100% 强一致性。

2. **跟进时间轴聚合（简单 - 核心）**
   * **业务实现**：拦截客户、商机状态变更的 API（或在 Service 层埋点），销售每次修改阶段时，强制要求传入跟进内容，并异步写入 `crm_record` 表，形成以 `related_id` 为轴心的时间线。

3. **商机漏斗推进（简单 - 核心）**
   * **业务实现**：控制商机阶段（需求分析 → 方案报价 → 商务谈判 → 赢单/输单）的单向流转。当状态变更为“赢单”时，后端返回特定标识，引导前端直接跳转至“新建合同”页面。

---

### 阶段三：资金与合规管控（中等优先级）
> **策略**：涉及金额与财务，难点在于数值的精确计算与多表状态的联动。

1. **合同与产品金额防篡改（中等难度 - 核心）**
   * **业务实现**：后端通过前端传来的 `product_id` 集合，去 `crm_product` 表实时查出标准单价，配合折扣率（`discount`）在后端使用 `BigDecimal` 重新计算总价，校验是否等于前端传来的 `total_amount`。
   * **合规逻辑**：若计算出的实际折扣低于 85 折，提交合同时自动将状态置为 `0-审批中`，并向其主管发送审批待办。

2. **回款计划与实收联动核销（中等难度 - 核心）**
   * **业务实现**：财务录入 `crm_receivable`（实际回款）时，关联 `crm_receivable_plan`（计划 ID）。
   * **联动逻辑**：单机环境下，直接利用 **Spring Event（ApplicationEventPublisher）** 发送一条“回款成功事件”。合同服务监听到事件后，累加该计划下的总实收。若实收款 $\ge$ 计划款，更新计划状态为已回款；同时检查当前合同下的所有计划是否均已完成，若完成则自动将 `crm_contract` 状态变更为 `2-已结束`。

---

### 阶段四：高级业务特性（低优先级，延后迭代）
> **策略**：属于旁路优化和提升协作效率的功能，可放在系统整体闭环跑通后再做。

1. **公海池自动回收任务（中等难度 - 非核心）**
   * **业务实现**：在单机架构中，直接在启动类开启 `@EnableScheduling`，并编写一个定时方法 `@Scheduled(cron = "0 0 2 * * ?")`（每天凌晨2点执行）。
   * **逻辑**：扫描 `crm_customer` 表中 `is_public = 0` 且 `last_follow_time` 距离当前时间超过 15 天的记录。将其 `owner_user_id` 置为 `NULL`，`is_public` 置为 `1`。
2. **跨部门客户共享放权（困难 - 非核心）**
   * **业务实现**：针对 `crm_customer_share` 表进行权限穿透。
   * **难点**：需要回头修改阶段一写好的“MyBatis-Plus 数据权限拦截器”。当普通销售查询客户列表时，拦截器拼装的 SQL 需从 `WHERE owner_user_id = 当前用户` 升级为：
     `WHERE owner_user_id = 当前用户 OR id IN (SELECT customer_id FROM crm_customer_share WHERE user_id = 当前用户)`。
     需要精细调试动态 SQL 解析器，防止产生 SQL 语法错误。

---

### 阶段五：跟进与报表（拆为两个独立 commit）

> **策略**：阶段五拆为两个可独立交付的 commit，**commit 1：跟进中心**（打通个人工作台）→ **commit 2：报表中心**（管理层决策支持）。两个 commit 之间可被任意打断、单独验收。
> **为什么不做阶段五再拆两个阶段**：阶段五两个模块都围绕"已沉淀数据二次利用"，工程量约 3-4 天，单独再分阶段会拖慢节奏。

---

#### Commit 1：跟进中心

##### 1) 业务目标
* 把侧边栏"跟进记录"空壳菜单变成**真实工作台页面**。
* 把 `RecordTimeline` 时间轴从仅客户详情**扩展到线索 / 商机 / 合同详情**（4 个实体统一跟进）。
* 提供"今日待跟进 / 本周待跟进（含逾期）/ 我的历史"3 个 Tab。
* 主布局顶部**铃铛 + 红点**逾期提醒（前端实现，零后端成本）。

##### 2) 后端实现
* **新增接口**（3 个）：
  * `GET /crm/record/todo/count` → 返回 `next_follow_time < NOW()` 且 owner = 当前用户的记录数。
  * `GET /crm/record/todo/list?type=today|week` → 待跟进列表，join 业务表（`crm_lead/customer/business/contract`）拿关联实体名称、负责人头像。
  * `GET /crm/record/mine?pageNum=&pageSize=` → 我写过的跟进分页，按 `create_time DESC`。
* **扩展接口**：
  * `GET /crm/record/timeline?relatedType=contract&relatedId=*` → `related_type` 枚举新增 `contract`，兼容老枚举值。
* **新增 VO**：`RecordTodoVO`（含关联实体名称、跟进方式、下次跟进距今天数、是否逾期等冗余字段，避免前端重复计算）。
* **权限码**：`crm:record:todo:list` / `crm:record:mine:list` / `crm:record:timeline:contract`（沿用原 `:list` 码，权限码拦截后再按 `relatedType` 在 Service 层判定业务可用性）。

##### 3) 前端实现
* **路由**：`/record/center` → `views/record/center.vue`，3 Tab 切换。
* **复用 + 扩展**：
  * `components/RecordTimeline.vue` 扩展 `relatedType` 支持 `business / contract`。
  * 在 `views/business/detail.vue`、`views/lead/detail.vue`、`views/contract/detail.vue` 中嵌入时间轴（**这 3 个详情页此前若未建，需在本 commit 一并新建最小可读骨架**）。
* **新增组件**：
  * `components/NotificationBell.vue`（铃铛 + 徽章，5 分钟轮询 `todo/count`）。
  * `views/record/components/RecordTodoCard.vue`（待跟进卡片，含实体类型 chip、关联名、跟进方式、距今天数）。
* **Pinia store**：`useTodoStore`（todoCount / fetchCount / markRead）。
* **挂载点**：在 `Layout/index.vue` 顶部工具栏插入 `<NotificationBell />`。

##### 4) SQL / 菜单变更
* `sys_menu` 新增 1 条：`跟进中心` → 路由 `/record/center`，权限码 `crm:record:center`。
* `sys_role_menu` 全部角色绑定（销售每天都要用）。
* **同步更新两份 SQL**：`sql/crm_full.sql`（全新安装）+ `sql/migrations/phase5-record.sql`（增量迁移，含 `DELETE + INSERT WHERE perms = 'crm:record:center'` 幂等模式）。
* **不动业务表**：`crm_record` 沿用，枚举值 `related_type` 在 Service 层做白名单校验，**不**改列类型。

##### 4.5) 线索-客户跟进迁移（v0.2 决策）
* **迁移规则**：`LeadService.convertToCustomer` 在 `@Transactional` 事务内追加两步：
  1. `UPDATE crm_record SET related_type='customer', related_id=#{newCustomerId} WHERE related_type='lead' AND related_id=#{leadId}`
  2. 保留原有"线索已转化为客户「xxx」"系统记录的 INSERT，但相关字段同步迁移到 customer 视角
* **新增迁移日志表**（**新增表结构，须同步两份 SQL**）：
  ```sql
  CREATE TABLE crm_record_migration_log (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    record_id     BIGINT       NOT NULL,
    from_type     VARCHAR(16)  NOT NULL,
    from_id       BIGINT       NOT NULL,
    to_type       VARCHAR(16)  NOT NULL,
    to_id         BIGINT       NOT NULL,
    migrate_time  DATETIME     DEFAULT CURRENT_TIMESTAMP,
    operator_id   BIGINT       NOT NULL,
    INDEX idx_record (record_id),
    INDEX idx_to (to_type, to_id)
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='跟进记录迁移日志';
  ```
* **新增权限码**：`crm:lead:mark_dead`、`crm:lead:read`（线索详情页基础权限，沿用现有 `crm:lead:*` 即可）

##### 4.6) 死线索实现（v0.2 决策）
* **crm_lead 表新增字段**（**走 phase5_add_col_if_missing 存储过程**，同步两份 SQL）：
  ```sql
  ALTER TABLE crm_lead
    ADD COLUMN dead_reason VARCHAR(255) DEFAULT NULL COMMENT '死因,可空',
    ADD COLUMN dead_time   DATETIME     DEFAULT NULL COMMENT '标记时间';
  ```
* **新增接口** `POST /api/crm/lead/{id}/markDead` body `{deadReason?: string}`：
  * 校验：当前用户是 `lead.owner_user_id`（防越权）；`lead.status` 必须 ∈ {1, 2}（未跟进/跟进中可标死，已转客户/已死不可重复标）。
  * 动作：UPDATE `crm_lead SET status=4, dead_time=NOW(), dead_reason=? WHERE id=?`。
  * 同步：写一笔系统跟进 `crm_record(relatedType='lead', content='线索标记为死线索:「xxx」', followType='system')`。
* **后端写跟进限制**：`RecordService.append()` 在白名单校验后追加：若 `relatedType='lead'` 且 `lead.status=4` → 抛 `BusinessException("死线索不允许写新跟进")`。
* **前端**：
  * `views/lead/detail.vue`（**本 commit 一并新建**，是 commit 1 的"详情页最小可读骨架"必建项之一）头部加"标记为死线索"按钮（owner 可见），点击弹 Dialog：死因下拉（不感兴趣 / 联系方式错误 / 已被竞品签下 / 预算不足 / 其他）+ 备注 textarea + 确认按钮。
  * 线索详情页 `status=4` 时：顶部红色 `el-alert` "该线索已死，不可写新跟进"；时间轴"+"按钮灰化 + tooltip。
  * 死线索时间轴可查看、可展开"系统记录"那条死因。
* **死线索列表筛选**：`lead/list.vue` 已有"已死线索"下拉筛选项，确认后端 `LeadQuery` DTO 已支持 `status=4`（状态机已就绪，仅新增字段）。

##### 4.7) 转客户软提示（v0.2 决策）
* 后端转客户接口**不动**（不强制卡点）。
* 前端 `views/lead/detail.vue` "转客户"按钮右侧加橙色 `el-alert` 警示条："⚠ 你还未跟进过，建议先写一条跟进"，按钮左对齐；点击"先去跟进"次级按钮直接弹出写跟进 Dialog。

##### 5) 本 Commit 不做
* 站内信 / 邮件 / 短信通知（纯前端红点）。
* 跟进统计分析报表（拆到 Commit 2）。
* 跟进记录的编辑 / 删除（业务上 append-only）。

---

#### Commit 2：报表中心

##### 6) 业务目标
* 4 张业务报表：销售漏斗+业绩 / 客户分布 / 跟进与转化率 / 回款+财务。
* **双入口**：侧边栏独立菜单 + Dashboard 卡片快入。
* 顶部时间范围 + 部门/销售筛选，所有报表联动重算。
* **本期所有人可见**，不叠加 `DataPermissionHandler` 数据权限拦截。

##### 7) 后端实现
* **新增 5 个聚合接口**：
  * `GET /crm/report/funnel?startDate=&endDate=&deptId=&userId=` → 销售漏斗（5 个阶段金额 + 商机数）。
  * `GET /crm/report/performance?startDate=&endDate=` → 月度业绩（合同金额、回款金额、按月分桶）。
  * `GET /crm/report/customer-distribution?dimension=industry|source|region` → 客户分布（按维度返回 Top 10 + 其他）。
  * `GET /crm/report/followup-conversion?startDate=&endDate=&deptId=&userId=` → 跟进与转化率（跟进频次 / 线索转客户率 / 商机赢率）。
  * `GET /crm/report/receivable-aging?asOfDate=` → 应收账款账龄（30 / 60 / 90 / 90+ 天分桶 + 合同回款进度）。
* **聚合 SQL 策略**：
  * **单 SQL + GROUP BY 维度**，避免 N+1 多次往返。
  * 时间范围统一在 Service 层转 `LocalDate` 区间，避免日期函数不能走索引。
  * 数据量大时 `LIMIT 500` 兜底（不让报表拖垮 DB）。
  * 索引补充：`crm_contract(sign_date)`、`crm_receivable(actual_time)`、`crm_business(stage, expected_deal_date)`。
* **新增 VO**：`FunnelVO` / `PerformanceVO` / `CustomerDistributionVO` / `FollowupConversionVO` / `ReceivableAgingVO`。
* **权限码**：`crm:report:funnel` / `crm:report:performance` / `crm:report:customer-distribution` / `crm:report:followup-conversion` / `crm:report:receivable-aging`。

##### 8) 前端实现
* **路由**：`/report` → `views/report/index.vue`，4 个 Tab（与后端 4 个报表一一对应）。
* **新增子组件**：
  * `views/report/components/FunnelChart.vue`（vue-echarts 漏斗图）。
  * `views/report/components/PerformanceChart.vue`（柱状+折线组合图）。
  * `views/report/components/CustomerDistributionChart.vue`（饼图 + 横向柱状图）。
  * `views/report/components/FollowupConversionChart.vue`（柱状图）。
  * `views/report/components/ReceivableAgingTable.vue`（Element Plus Table + 横向进度条）。
* **复用筛选条**：抽象 `components/ReportFilterBar.vue`（时间范围 + 部门/销售下拉树 + 应用按钮）。
* **Pinia 缓存**：`useReportStore` 按 `(reportKey + filterHash)` 缓存 5 分钟，避免切换 Tab 重复请求。
* **Dashboard 改造**：`views/dashboard/index.vue` 增加 4 张报表入口卡片，点击跳转 `/report?tab=funnel` 之类。

##### 9) SQL / 菜单变更
* `sys_menu` 新增 1 条：`报表中心` → 路由 `/report`，权限码 `crm:report:*`。
* `sys_role_menu` 全部角色绑定。
* **同步更新两份 SQL**：`sql/crm_full.sql` + `sql/migrations/phase5-report.sql`（同样 `DELETE + INSERT WHERE perms IN ('crm:report:funnel', ...)` 幂等模式）。
* **新建索引**：上述 3 个索引列在两份 SQL 中同步 `CREATE INDEX IF NOT EXISTS`（按阶段迁移脚本约定走 `phase5_add_index_if_missing` 存储过程）。

##### 10) 本 Commit 不做
* 数据权限拦截（所有人可见，权限分层列入阶段六）。
* Excel / CSV 导出。
* 报表订阅、邮件推送、定时报表。
* 报表快照表（聚合查询实时跑，避免数据不一致）。

##### 11) 验收标准
* 4 个报表接口 P95 响应 < 1s（参考值，超出加索引）。
* 报表页面切换 Tab 无白屏（store 缓存命中）。
* 时间范围 / 部门筛选实时联动，4 张图同步重算。
* 角色权限：未登录用户访问 `/report` → 401。

##### 12) 风险与对策
| 风险 | 等级 | 对策 |
| :--- | :--- | :--- |
| 聚合 SQL 拖垮 DB | 中 | 索引补充 + LIMIT 500 + 前端 5min 缓存 |
| 部门/销售树形数据来源 | 低 | 复用阶段一 `sys_dept` / `sys_user` 表，前端缓存组织树 |
| ECharts 主题统一 | 低 | 复用 Dashboard 阶段二已沉淀的 echarts theme，禁止组件内硬编码色值 |
| 大数据量报表导出 | 低 | 本期不做导出，留出"导出"按钮位但 disabled 加 tooltip 提示"暂未开放" |

---

### 阶段五交付物清单（验收用）

* **代码**：`backend/` + `frontend/` 新增 / 修改文件清单。
* **接口文档**：`docs/api/record.md`（更新）+ `docs/api/report.md`（新建），含 `README.md` 索引同步。
* **SQL 脚本**：`sql/crm_full.sql` + `sql/migrations/phase5-record.sql` + `sql/migrations/phase5-report.sql`，均经"全新安装 + 增量幂等"两轮实测。
* **冒烟测试**：`backend-tools/` 下补充跟进中心 + 报表中心接口的 curl 冒烟脚本。
* **联调确认**：前端本地启动后铃铛红点、跟进中心 3 Tab、商机/线索/合同详情时间轴、Dashboard 4 卡片、报表中心 4 Tab 全流程跑通。

---

### 阶段六：系统设置 + 产品管理（拆为两个独立 commit）

> **策略**：阶段六拆为 2 个 commit（与阶段五节奏一致）：**commit 1：系统设置**（user/role/menu/dept 4 子模块 + sidebar 第 5 组"管理" + 14 条新权限码），**commit 2：产品管理**（已有 CRUD + category 补齐 + SaaS 字段升级 + sidebar 交易组接入）。两个 commit 之间可被任意打断、单独验收。

---

#### Commit 1：系统设置

##### 1) 业务目标
* 把 router 中 4 个 `/system/*` 占位页（user/role/menu/dept）从"一张空卡"变成真实 CRUD 页面。
* 补齐 12 条新 `sys_menu` 权限码（系统设置，router 已声明但 DB 没建——历史欠账）。
* 让 admin / 销售总监可以在前台完成"开账号 / 调权限 / 改组织 / 重置密码"日常运维，不再依赖 SQL 直连。
* 新增侧边栏第 5 分组"管理"（admin + 销售总监 2 角色可见），解决当前管理入口缺失。

##### 2) 后端实现

###### 2.1 新增 / 扩展文件清单
| 路径 | 类型 | 说明 |
| :--- | :--- | :--- |
| `entity/SysDept.java` | **新建** | 当前项目无 Dept 的 Java 类（阶段一漏建） |
| `entity/SysUser.java` | **已存在** | 不动；扩展 `@TableLogic` 已就绪 |
| `entity/SysRole.java` | **已存在** | 不动 |
| `entity/SysMenu.java` | **已存在** | 不动 |
| `mapper/SysDeptMapper.java` | **新建** | 基础 selectList/selectById/insert/update/deleteById |
| `mapper/SysUserMapper.java` | **已存在** | 扩展 `selectUserPage(keyword,deptId,status)` |
| `mapper/SysRoleMapper.java` | **已存在** | 扩展 `selectRolePage(keyword)` |
| `mapper/SysMenuMapper.java` | **已存在** | 扩展 `selectMenuTree()`（按 parent_id 排序） |
| `controller/SysUserController.java` | **扩展** | 阶段四仅有 listUsers；本期重写为完整 CRUD + 重置密码 + 分配角色 |
| `controller/SysRoleController.java` | **新建** | 完整 CRUD + 绑定菜单 |
| `controller/SysMenuController.java` | **新建** | 完整 CRUD + 树形返回 |
| `controller/SysDeptController.java` | **新建** | 完整 CRUD + 树形返回 |
| `service/SysUserService.java` | **新建** | 拆出原 Controller 里的内联逻辑 |
| `service/SysRoleService.java` | **新建** | 含全量重绑 sys_role_menu 逻辑 |
| `service/SysMenuService.java` | **新建** | 含父子一致性 / 类型校验 |
| `service/SysDeptService.java` | **新建** | 含 `ancestors` 自动维护 |
| `dto/SysUserCreateRequest.java` | **新建** | 含 roles: List<Long> |
| `dto/SysUserUpdateRequest.java` | **新建** | roles 可选 |
| `dto/SysRoleCreateRequest.java` | **新建** | 含 menuIds: List<Long> |
| `dto/SysDeptCreateRequest.java` | **新建** | |
| `dto/SysMenuCreateRequest.java` | **新建** | |
| `vo/SysUserVO.java` | **新建** | 联表 deptName / roleNames |
| `vo/SysRoleVO.java` | **新建** | 含 menuIds / dataScopeText |
| `vo/SysMenuVO.java` | **新建** | 树形 children |
| `vo/SysDeptVO.java` | **新建** | 树形 children + userCount |

###### 2.2 新增接口（路由前缀 `/api/sys`）
| 方法 | 路径 | 权限码 | 用途 |
| :--- | :--- | :--- | :--- |
| GET | `/sys/user/page` | `sys:user:list` | 用户分页（联 deptName / roleNames） |
| GET | `/sys/user/{id}` | `sys:user:list` | 用户详情（含 roles 列表） |
| POST | `/sys/user` | `sys:user:edit` | 新建用户（默认密码 123456） |
| PUT | `/sys/user` | `sys:user:edit` | 更新用户（username 不可改） |
| DELETE | `/sys/user/{id}` | `sys:user:edit` | 逻辑删除 + Sa-Token 踢下线 |
| POST | `/sys/user/{id}/resetPassword` | `sys:user:reset_pwd` | 重置密码（admin 视角，无旧密码） |
| PUT | `/sys/user/{id}/roles` | `sys:user:assign_role` | 全量重绑 sys_user_role |
| PUT | `/sys/user/{id}/status` | `sys:user:edit` | 启停用（status 切换） |
| GET | `/sys/role/page` | `sys:role:list` | 角色分页 |
| GET | `/sys/role/all` | `sys:role:list` | 角色下拉（所有 status=1） |
| POST | `/sys/role` | `sys:role:edit` | 新建角色 |
| PUT | `/sys/role` | `sys:role:edit` | 更新角色 |
| DELETE | `/sys/role/{id}` | `sys:role:edit` | 删除（内置 5 角色保护 + 校验无用户绑定） |
| PUT | `/sys/role/{id}/menus` | `sys:role:assign_menu` | 全量重绑 sys_role_menu |
| GET | `/sys/menu/tree` | `sys:menu:list` | 菜单树（按 parent_id） |
| POST | `/sys/menu` | `sys:menu:edit` | 新建菜单（类型校验） |
| PUT | `/sys/menu` | `sys:menu:edit` | 更新菜单 |
| DELETE | `/sys/menu/{id}` | `sys:menu:edit` | 删除（无子菜单 + 无角色绑定） |
| GET | `/sys/dept/tree` | `sys:dept:list` | 部门树 |
| POST | `/sys/dept` | `sys:dept:edit` | 新建部门（ancestors 自动拼装） |
| PUT | `/sys/dept` | `sys:dept:edit` | 更新部门 |
| DELETE | `/sys/dept/{id}` | `sys:dept:edit` | 删除（无子部门 + 无用户归属） |

###### 2.3 关键业务规则
* **`ancestors` 自动维护**：`SysDeptService.create/update` 内根据 `parent.ancestors + "," + parent.id` 拼装，**不依赖 SQL 触发器**。
* **角色权限全量重绑**：`SysRoleService.assignMenus(roleId, menuIds)` → 同一事务内 `DELETE FROM sys_role_menu WHERE role_id=?` + 批量 `INSERT`，**不做"diff 增量"**（与阶段四 `crm_customer_share` 风格一致，简单可控）。
* **重置密码**：`POST /sys/user/{id}/resetPassword?newPassword=xxx`，默认 `123456`；后端 `passwordEncoder.encode()` 落库。
* **admin 自保护**：所有写接口的 Service 层首行 `if (StpUtil.getLoginId().equals(targetId)) throw new BusinessException("不能操作自己")`。
* **admin 至少 1 人**：DELETE 用户 / status=0 切换时，`SELECT count(*) FROM sys_user WHERE id IN (sys_user_role.user_id) AND role_id=1 AND status=1 AND is_deleted=0` ≥ 1。
* **内置 5 角色不可删**：`DELETE /sys/role/{id}` 首行 `if (role.role_key IN ('admin','sales_director','sales_lead','sales','finance')) throw`。
* **种子 admin + 销售总监绑定**：12 条系统设置新 `sys_role_menu` 记录插入时，**绑 admin（role_id=1）和销售总监（role_id=2）共 2 个角色**，主管/销售/财务一个都不绑（与 router `meta.roles:['admin','sales_director']` 一致）。

##### 3) 前端实现

###### 3.1 新增 / 重写文件
| 路径 | 说明 |
| :--- | :--- |
| `views/system/user.vue` | **重写**：表格 + Dialog（编辑/重置密码/分配角色 Dialog 复用同一表单） |
| `views/system/role.vue` | **重写**：表格 + Dialog（含菜单权限 el-tree 复选） |
| `views/system/menu.vue` | **重写**：树形表格 + 行内编辑 |
| `views/system/dept.vue` | **重写**：树形表格（平铺+缩进风格） |
| `api/sys-user.js` | **新建** | 12 个接口 |
| `api/sys-role.js` | **新建** | 6 个接口 |
| `api/sys-menu.js` | **新建** | 4 个接口 |
| `api/sys-dept.js` | **新建** | 4 个接口 |
| `composables/useUserRole.js` | **新建** | 提取 admin 角色判断 `isAdmin` 给 SidebarMenu 用 |

###### 3.2 关键交互
* **用户管理页**：表格列 `账号 / 昵称 / 部门 / 角色(多 chip) / 状态 / 创建时间 / 操作`；操作列按钮 `编辑 / 重置密码 / 启停用 / 删除`。
* **角色权限绑定 Dialog**：内嵌 `el-tree` 复选树，节点 type=`M/C` 父子联动；底部"全选/全清"快捷按钮。
* **菜单管理页**：`el-table` 默认展开所有父级；新增/编辑 Dialog 含类型下拉（M/C/F 切换时动态显隐 `path/component/perms` 必填项）。
* **部门管理页**：与菜单同款树形，编辑时父部门下拉"不能选自己 / 自己的子孙"（前端用递归过滤）。

###### 3.3 SidebarMenu.vue 改造
* 新增第 5 分组 `管理`（admin + 销售总监 2 角色可见）：
  ```js
  {
    label: '管理',
    requiresRole: ['admin', 'sales_director'],  // SidebarMenu 新增字段,useUserStore.roleKey 校验
    children: [
      { path: '/system/user', title: '用户管理', icon: 'UserFilled', perm: 'sys:user:list' },
      { path: '/system/role', title: '角色管理', icon: 'Avatar',    perm: 'sys:role:list' },
      { path: '/system/menu', title: '菜单权限', icon: 'Menu',      perm: 'sys:menu:list' },
      { path: '/system/dept', title: '部门管理', icon: 'OfficeBuilding', perm: 'sys:dept:list' }
    ]
  }
  ```
* 与 `useUserStore.roleKey` 联动（`v-if="['admin','sales_director'].includes(userStore.roleKey)"` 渲染整组），其他 3 个角色用户**完全看不到**这一组。

##### 4) SQL / 菜单变更

###### 4.1 新增 sys_menu 记录（系统设置 12 条 + 产品分类 2 条 = 14 条）
| id | menu_name | path | component | menu_type | perms | parent_id | order_num | 归属 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 24 | 管理 | /system | layout | M | sys:system:view | 0 | 35 | commit 1 |
| 25 | 用户管理 | system/user | system/user | C | sys:user:list | 24 | 1 | commit 1 |
| 26 | 用户编辑 | — | — | F | sys:user:edit | 24 | 2 | commit 1 |
| 27 | 重置密码 | — | — | F | sys:user:reset_pwd | 24 | 3 | commit 1 |
| 28 | 分配角色 | — | — | F | sys:user:assign_role | 24 | 4 | commit 1 |
| 29 | 角色管理 | system/role | system/role | C | sys:role:list | 24 | 5 | commit 1 |
| 30 | 角色编辑 | — | — | F | sys:role:edit | 24 | 6 | commit 1 |
| 31 | 分配菜单 | — | — | F | sys:role:assign_menu | 24 | 7 | commit 1 |
| 32 | 菜单权限 | system/menu | system/menu | C | sys:menu:list | 24 | 8 | commit 1 |
| 33 | 菜单编辑 | — | — | F | sys:menu:edit | 24 | 9 | commit 1 |
| 34 | 部门管理 | system/dept | system/dept | C | sys:dept:list | 24 | 10 | commit 1 |
| 35 | 部门编辑 | — | — | F | sys:dept:edit | 24 | 11 | commit 1 |
| 36 | 产品分类 | product/category | product/category | C | crm:product:category:list | 0 | 22 | commit 2 |
| 37 | 产品分类编辑 | — | — | F | crm:product:category:edit | 0 | 23 | commit 2 |

> **拆分原则**：14 条菜单按"模块归属"分散到 2 个 migration 脚本。
> * **`phase6-system.sql`**（commit 1）：只装 24-35 共 12 条系统设置菜单 + admin/sales_director 绑定。
> * **`phase6-product.sql`**（commit 2）：只装 36-37 共 2 条产品分类菜单 + 全员 5 角色绑定。
> 这样 commit 1 可以独立验收、commit 2 不依赖 commit 1 也可单独升级（生产环境按节奏分批上线）。
> 全新安装路径 `crm_full.sql` 一次性包含全部 14 条 + 全部绑定。

###### 4.2 新增 sys_role_menu 绑定
* **系统设置 12 条（commit 1 装）**：
    * admin（role_id=1）：全绑 12 条。
    * 销售总监（role_id=2）：全绑 12 条。
    * 销售主管/销售/财务（role_id=3/4/5）：0 条。
* **产品分类 2 条（commit 2 装）**：
    * admin/销售总监/销售主管/销售/财务 **5 角色全绑**（产品分类是公共资源）。
* 走 `DELETE + INSERT WHERE perms LIKE 'sys:%' AND role_id IN (1,2)` 幂等模式（老环境可能已有部分 sys:* 菜单散落，限定 role_id 防误删其他角色绑定）。

###### 4.3 SQL 同步脚本
* **`sql/crm_full.sql`** 在阶段五 3 个菜单块后追加 14 条新菜单（id 24-37） + sys_role_menu 完整绑定（admin+总监 12 条 / 全员 2 条）。
* **`sql/migrations/phase6-system.sql`**（**新增**）：
    * 用 `phase6_add_col_if_missing` 存储过程补 **0 个字段**（本 commit 1 不动业务表结构——只是新建 Java 类）。
    * `DELETE + INSERT WHERE perms LIKE 'sys:%' AND role_id IN (1,2)` 幂等模式插入 12 条系统设置菜单与 admin+sales_director 绑定。
* **`sql/migrations/phase6-product.sql`**（**新增，commit 2 时建**）：
    * 用 `phase6_add_col_if_missing` 存储过程给 `crm_product` 加 2 列（`product_line` / `billing_cycle`）。
    * `DELETE + INSERT WHERE perms IN ('crm:product:category:list', 'crm:product:category:edit')` 幂等模式插入 2 条产品分类菜单与 5 角色全绑。
* **验证流程**（两脚本独立验）：
  ```bash
  # 1) 全新安装
  mysql -u root -p123456 -e "DROP DATABASE IF EXISTS crm_db; CREATE DATABASE crm_db;"
  mysql -u root -p123456 crm_db < sql/crm_full.sql

  # 2) 增量迁移 commit 1
  mysql -u root -p123456 crm_db < sql/migrations/phase6-system.sql  # 第一次
  mysql -u root -p123456 crm_db < sql/migrations/phase6-system.sql  # 第二次(应无错)

  # 3) 增量迁移 commit 2(在 #2 之后跑,或独立从阶段五环境起跑均可)
  mysql -u root -p123456 crm_db < sql/migrations/phase6-product.sql  # 第一次
  mysql -u root -p123456 crm_db < sql/migrations/phase6-product.sql  # 第二次(应无错)
  ```

##### 5) 风险与对策
| 风险 | 等级 | 对策 |
| :--- | :--- | :--- |
| admin 误把自己禁用锁死系统 | 高 | 后端"admin 至少 1 人"校验 + 前端按钮灰化 + admin 不能操作自己 |
| 14 条新权限码老用户不感知 | 中 | 通知所有 admin 重新登录刷新 localStorage（见 [[crm-user-permissions-cache-pitfall]]） |
| sys_menu 树形前端性能 | 低 | 菜单量 < 100 条，平铺渲染够用，无需虚拟滚动 |
| 内置 5 角色被误删 | 中 | 后端 role_key 硬编码白名单拦截 |
| SysDept 跨表 ancestors 维护 | 低 | Service 层集中拼装，禁止 SQL 触发器（参考阶段四客户共享的"代码层可控"风格） |

##### 6) 本 Commit 不做
* 字典管理（sys_dict）— 业务暂未用到。
* 审计日志 / 登录日志 — 留待阶段七。
* 用户头像 / 个人中心 / 主题切换 — 与本阶段主题无关。
* 部门批量导入 Excel — admin 体量小，手动更可控。

---

#### Commit 2：产品管理（SaaS 升级 + 分类补齐 + sidebar 接入）

##### 7) 业务目标
* 把现有 `crm_product` 单层扁平模型升级为 **SaaS 多版本产品**（加 `product_line` / `billing_cycle`）。
* 补齐 `crm_product_category` 的 Java 类与前端页面（表已建无 Service）。
* 把"产品"接入 sidebar 交易组（合同/回款/产品 三个子项）。
* 让销售在签合同时一眼看到产品版本与计费周期（合同新建页产品下拉 chip 化）。

##### 8) 后端实现

###### 8.1 新增 / 扩展文件
| 路径 | 类型 | 说明 |
| :--- | :--- | :--- |
| `entity/CrmProduct.java` | **扩展** | 加 `productLine` / `billingCycle` 字段 |
| `entity/CrmProductCategory.java` | **新建** | 平铺分类 |
| `mapper/CrmProductCategoryMapper.java` | **新建** | |
| `controller/ProductCategoryController.java` | **新建** | `/api/crm/product/category` 4 个 CRUD |
| `service/ProductCategoryService.java` | **新建** | 含"有产品引用时禁止删除"校验 |
| `dto/ProductCategoryCreateRequest.java` | **新建** | categoryName |
| `dto/ProductCategoryUpdateRequest.java` | **新建** | |
| `vo/ProductCategoryVO.java` | **新建** | |
| `vo/ProductVO.java` | **扩展** | 加 `productLine` / `billingCycle` / `categoryName` / `productLineText` / `billingCycleText` |
| `dto/ProductCreateRequest.java` | **扩展** | 加 productLine / billingCycle（默认 基础版/年） |
| `dto/ProductUpdateRequest.java` | **扩展** | |
| `dto/ProductQueryRequest.java` | **扩展** | 加 productLine / billingCycle 过滤 |
| `service/ProductService.java` | **扩展** | toVO 联表查 categoryName（见 [[crm-service-tovo-name-map-pattern]]） |

###### 8.2 新增接口（路由前缀 `/api/crm/product/category`）
| 方法 | 路径 | 权限码 | 用途 |
| :--- | :--- | :--- | :--- |
| GET | `/crm/product/category/list` | `crm:product:category:list` | 全量列表（status=1，max 200） |
| GET | `/crm/product/category/all` | `crm:product:category:list` | 全量下拉（key/value） |
| POST | `/crm/product/category` | `crm:product:category:edit` | 新建分类 |
| PUT | `/crm/product/category` | `crm:product:category:edit` | 更新分类 |
| DELETE | `/crm/product/category/{id}` | `crm:product:category:edit` | 删除（有产品引用时拒绝） |

> 既有 `GET /crm/product/page` 等 5 个接口**全部保留**，仅 `ProductVO` / `ProductCreateRequest` / `ProductUpdateRequest` / `ProductQueryRequest` 加字段，向前兼容（老产品数据 productLine/billingCycle 走 DB DEFAULT）。

##### 9) 前端实现

###### 9.1 新增 / 修改文件
| 路径 | 说明 |
| :--- | :--- |
| `views/product/category.vue` | **新建**：平铺列表 + 增删改 Dialog + 引用计数 |
| `views/product/list.vue` | **升级**：加 categoryName / productLine(chip) / billingCycle 3 列 + 3 个筛选下拉 |
| `views/contract/submit.vue` | **升级**：产品下拉 chip 化显示"ZenCRM 企业版（旗舰版/年）" |
| `api/product-category.js` | **新建** | 5 个接口 |
| `api/product.js` | **扩展** | 既有 5 个接口参数加 productLine / billingCycle |

###### 9.2 关键交互
* **产品分类页**：表格列 `分类名称 / 创建时间 / 引用产品数 / 操作`；引用产品数 = `SELECT count(*) FROM crm_product WHERE category_id=? AND is_deleted=0`；操作列"删除"按钮在引用数 > 0 时灰化。
* **产品列表页**：3 个新筛选下拉 `分类（来自 category/list）` / `套餐线（基础版/专业版/旗舰版）` / `计费周期（月/季/年）`；新增列 chip 配色（基础=灰 / 专业=蓝 / 旗舰=金），与产品详情 Dialog 同步。
* **合同新建页**：产品 `el-select` 自定义模板 `<option>` 内显示 chip 套餐线 + 计费周期，让销售一眼识别。

###### 9.3 SidebarMenu.vue 改造
* 交易组加产品子项：
  ```js
  {
    label: '交易',
    children: [
      { path: '/contract/list', title: '合同', icon: 'Document' },
      { path: '/contract/receivable', title: '回款', icon: 'Money' },
      { path: '/product/list', title: '产品', icon: 'Goods', perm: 'crm:product:list' },
      { path: '/product/category', title: '产品分类', icon: 'Files', perm: 'crm:product:category:list' }
    ]
  }
  ```
* **不创建**顶级"产品管理"目录（与 [[crm-zen-no-horizontal-topbar]] 警示一致）。

##### 10) SQL / 菜单变更

###### 10.1 crm_product 表加 2 列（迁移走 `phase6_add_col_if_missing`）
```sql
CALL phase6_add_col_if_missing('crm_product', 'product_line',  "VARCHAR(32)  DEFAULT '基础版' COMMENT '套餐线'");
CALL phase6_add_col_if_missing('crm_product', 'billing_cycle',"VARCHAR(16)  DEFAULT '年'     COMMENT '计费周期'");
```
* `crm_full.sql` 同步在 `crm_product` 表 DDL 内追加 2 列。
* `sql/migrations/phase6-product.sql`（**新增**）：上面 2 行 `phase6_add_col_if_missing` 调用。
* **索引**：不新增索引（`product_line` / `billing_cycle` 仅用于下拉过滤，数据量 < 1000 条，全表扫描无压力）。

###### 10.2 菜单与权限（commit 1 阶段已完成 14 条，本 commit 2 只需在 admin 上多绑 2 条）
* `sys_role_menu` 在 admin（role_id=1）补绑 36/37（产品分类菜单）。
* 销售总监 / 主管 / 销售 / 财务**全部**也补绑 36/37（产品分类是公共资源，所有人都要看）。
* 走 `DELETE + INSERT WHERE perms IN ('crm:product:category:list', 'crm:product:category:edit')` 幂等模式。

##### 11) 风险与对策
| 风险 | 等级 | 对策 |
| :--- | :--- | :--- |
| 现有种子产品 product_line 为空 | 低 | DB DEFAULT `'基础版'`，老数据自动回填 |
| 既有 /crm/product/page 返回结构变化破坏合同页 | 低 | ProductVO 仅**加字段不删字段**，向前兼容（前端按字段存在性渲染） |
| 阶段三合同金额反推逻辑受影响 | 低 | contract_product.sales_price 仍按 `price × count` 重算，与 product_line/billing_cycle 无关 |
| 分类引用计数与实际产品数不一致 | 中 | 走 `SELECT count(*)` 实时查询，不缓存 |

##### 12) 本 Commit 不做
* 多档价格表 / 座位数上下限 / 试用期产品 / 产品图片。
* 产品 SKU 自动编码生成器（人工填 + DB 唯一索引兜底）。
* 产品导入导出 Excel（admin 体量小）。
* "产品统计报表"（如各套餐线销售占比）— 留待阶段七报表中心增强。

---

### 阶段六交付物清单（验收用）

* **代码**：
    * commit 1：6 个新 Java 类（SysDept / 4 个 Service / SysDeptMapper）+ 4 个新 Controller + 3 个新 Service（User/Role/Menu）+ 9 个新 DTO/VO + 4 个 Vue 全量重写 + 5 个新 api js + 1 个 composable + SidebarMenu.vue 改造。
    * commit 2：1 个新 entity + 1 个新 mapper + 1 个新 controller + 1 个新 service + 6 个新 DTO/VO + 2 个 Vue 改写（category 新建 + list 升级）+ 1 个新 api js + submit.vue 升级。
* **接口文档**：
    * `docs/api/sys-user.md`（新建）+ `docs/api/sys-role.md`（新建）+ `docs/api/sys-menu.md`（新建）+ `docs/api/sys-dept.md`（新建）。
    * `docs/api/product.md`（更新，加 category 5 个接口 + product 字段说明）。
    * `docs/api/README.md`（更新索引）。
* **SQL 脚本**：`sql/crm_full.sql`（同步 14 菜单 + 12 admin 绑 + 2 角色绑 + 2 字段）+ `sql/migrations/phase6-system.sql`（新建）+ `sql/migrations/phase6-product.sql`（新建），均经"全新安装 + 增量幂等"两轮实测。
* **冒烟测试**：`backend-tools/` 补充系统设置 + 产品分类 + 产品 SaaS 字段接口的 curl 冒烟脚本。
* **联调确认**：
    * commit 1：admin 登录后看到侧边栏"管理"分组，4 个子页全功能跑通；非 admin 用户完全看不到"管理"组。
    * commit 2：admin 看到交易组多出"产品"+"产品分类"；销售新建合同时产品下拉显示套餐线与计费周期 chip。
    * 阶段三既有合同新建/金额校验流程**回归通过**（不能因为加字段搞坏老逻辑）。

> **v0.3 决定（2026-06-29）**：阶段六做完即收官，**不开阶段七**。详见 memory `crm-phase6-scope-final.md`。

---

## 三、 长期 Backlog（v0.3 终稿，6 项，不承诺具体阶段）

> **6 人公司决策**：字典管理、审计日志两类"基础治理"特性经用户拍板**不做**（投入产出比偏低，硬编码 + update_by/update_time 已够用）；其余 4 项也暂未排期。

* **字典管理（sys_dict）** — 不做。硬编码下拉 + `el-option` 够用，引入新表反而要维护 6 处下拉数据源同步。
* **审计日志（sys_operation_log）** — 不做。6 人公司 admin/销售总监彼此熟悉，update_by+update_time 即可定位；审计表 + 拦截器 + 90 天清理 Job 运维成本高于价值。
* **通知公告**（站内公告 + 红点 + 弹窗）
* **个人中心**（昵称/邮箱/手机/头像/主题切换）
* **角色/部门批量导入导出 Excel**
* **登录日志**（登录时间/IP/设备/失败次数统计 + 失败锁定）

附（不承诺）：
* 报表中心数据权限分层（V1 所有人可见 → V2 按 data_scope 分层）
* 各业务表的批量导入（线索 Excel 阶段四已做；客户/合同/产品的 Excel 导入）